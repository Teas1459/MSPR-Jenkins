pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git 'https://github.com/Teas1459/MSPR-Jenkins'
                $ java /path/application fichiergihub
            }
        }
    }
}
